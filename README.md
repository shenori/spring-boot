JDBC(Java Database Connectivity) we want this to connect java with database
we can coonect any sql (mariadb, Mysql,Oracle,etc) with JDBC.

Jaca has another API  called JPA(Java Persistance API) Intergrated with ORM concept
Embedded abstraction layer top of JDBC point out declarative programming model
simlify the DB connectivity get supports for serval venders hight levels rather than JDBC.

MySQL is open source RDBMS , structured Query Lanuage(stroing, retrieving, managing data.),
cross platform support(runs linux, windows, masOS and more) 
